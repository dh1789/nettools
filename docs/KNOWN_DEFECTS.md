# KNOWN_DEFECTS.md — nettools 결함 카탈로그

> `bin/harness defects` 가 이 문서를 파싱한다.
> 형식: `### {ID}.` 헤더 + 본문에 `**심각도**: HIGH|MEDIUM|LOW` + 해결 시 본문에 `✅ 해결됨` 표기.
> ID 규칙: `N-1`, `N-2`, ... (nettools 의 N).

---

## 미해결 결함

(현재 추적 중인 미해결 결함 없음 — 새 결함 발견 시 아래 형식으로 추가)

---

## 결함 추가 템플릿

```markdown
### N-X. <한 줄 요약>

**심각도**: HIGH | MEDIUM | LOW
**최초 발견**: YYYY-MM-DD
**영향 범위**: <어떤 사용자/시나리오가 영향받는가>

**증상**:
<재현 가능한 증상>

**원인**:
<루트 원인 분석>

**임시 회피**:
<있다면 — 코드/명령어>

**근본 해결 계획**:
<구조적 변경 / 리팩터 / PR 링크>

**관련 함정**: HARNESS_ENGINEERING.md §14 의 TR-N (있다면)
```

---

## 해결됨 (참고용 — 일정 기간 후 archive 로 이동)

(아직 없음)
