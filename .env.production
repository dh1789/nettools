# 프로덕션 빌드 환경변수 (Cloudflare Pages `npm run build`가 읽음)
# NEXT_PUBLIC_* 는 클라이언트 번들에 노출되는 공개값 (비밀 아님).
# AdSense publisher ID — layout.tsx가 이 값으로 adsbygoogle.js 스크립트를 주입.
NEXT_PUBLIC_ADSENSE_ID=ca-pub-6842181130818480
